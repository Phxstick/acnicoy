appId: "com.electron.acnicoy"
productName: "Acnicoy"
artifactName: "${productName}-${version}-${os}-${arch}.${ext}"
publish: "github"
win:
  target:
    - "zip"
    - "nsis"
linux:
  category: "Education"
  target:
    - "tar.gz"
    - "deb"
