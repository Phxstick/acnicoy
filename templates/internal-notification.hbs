<div class="notification">
  <div class="upper">
    <div class="info-frame">
      <div class="title">{{{title}}}</div>
      {{#if subtitle.length}}
        <div class="subtitle">{{{subtitle}}}</div>
      {{/if}}
    </div>
    {{#if buttonLabel.length}}
       <button class="action">{{{buttonLabel}}}</button>
    {{/if}}
  </div>
  <div class="details">{{{details}}}</div>
</div>
